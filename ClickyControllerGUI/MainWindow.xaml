<Window x:Class="ClickyControllerGUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClickyControllerGUI"
        xmlns:viewmodel="clr-namespace:ClickyControllerGUI.ViewModels"
        mc:Ignorable="d"
        Title="Clicky Controller" Height="450" Width="800">

    <Window.Resources>
        <viewmodel:ScriptExecutor x:Key="ScriptExecutor"/>
    </Window.Resources>
    
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" DataContext="{Binding Source={StaticResource ScriptExecutor}}">
            <Label>Command List</Label>
            <ListView ItemsSource="{Binding CommandList}" BorderBrush="Black"/>
        </StackPanel>
    </Grid>

    
</Window>
